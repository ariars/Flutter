import 'package:flutter/material.dart';

// 배경색
const backgroundColor = Color(0xFF0E0E0E);

// 주색상
const primaryColor = Colors.white;

//보조색상 (600 키값 때문에 const 사용 불가)
final secondaryColor = Colors.green[600];
